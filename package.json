{
  "name": "noterama",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "pages:build": "npx @cloudflare/next-on-pages --cwd apps/web"
  }
}
